# =============================================================================
# projeto_01 — Recomendador de Municípios Brasileiros por Perfil Agropecuário
# =============================================================================
# Este arquivo é um espelho conveniente das dependências declaradas no
# pyproject.toml. A instalação canônica é feita via:
#
#   pip install -e ".[notebook,dev]"
#
# Este requirements.txt existe para permitir 'pip install -r requirements.txt'
# como alternativa rápida, útil para quem não quer instalação editável.
# =============================================================================

# -----------------------------------------------------------------------------
# Núcleo pedagógico do projeto DSA original (álgebra linear, NLP básico, ML)
# -----------------------------------------------------------------------------
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.4,<2.0
nltk>=3.9,<4.0

# -----------------------------------------------------------------------------
# Coleta de dados IBGE (fonte principal do dataset)
# -----------------------------------------------------------------------------
sidrapy>=0.1.4,<0.2
requests>=2.32,<3.0

# -----------------------------------------------------------------------------
# Utilidades gerais do pipeline
# -----------------------------------------------------------------------------
python-dotenv>=1.0,<2.0
tqdm>=4.67,<5.0
pyarrow>=15.0

# -----------------------------------------------------------------------------
# Notebooks, visualização, EDA
# -----------------------------------------------------------------------------
jupyter>=1.1
jupyterlab>=4.4
ipywidgets>=8.1
matplotlib>=3.9
seaborn>=0.13
watermark>=2.4

# -----------------------------------------------------------------------------
# Testes e qualidade de código
# -----------------------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5

# -----------------------------------------------------------------------------
# Instalação editável do próprio pacote
# -----------------------------------------------------------------------------
-e .

# -----------------------------------------------------------------------------
# Extensão espacial (mestrado) — instalação sob demanda
# -----------------------------------------------------------------------------
# Descomente as linhas abaixo quando for trabalhar a extensão de validação
# espacial com Moran's I. No Windows sem conda, essas libs às vezes têm
# problemas de build; se falhar, sugere-se instalar via conda-forge.
#
# geopandas>=1.0
# libpysal>=4.10
# esda>=2.5
# contextily>=1.6
